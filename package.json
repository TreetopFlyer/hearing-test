{
  "name": "hearing-test",
  "scripts": {
    "go": "esbuild ./src/App.tsx --minify --bundle --watch --sourcemap --outfile=./dist/out.js"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.25"
  }
}
